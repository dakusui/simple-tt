source "$(dirname "$(dirname "${BASH_SOURCE[0]}")")/lib/config.rc"

function execute_stage() {
  rm -fr "$(project_root)/autotest/target/testResult"
}

function describe_stage() {
  echo 'This removes all the test results generated by "autotest" tool. ('"$(project_root)/autotest/target"')'
}
