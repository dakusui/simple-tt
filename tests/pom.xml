<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.github.dakusui</groupId>
  <artifactId>tests_simple-tt</artifactId>
  <packaging>jar</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>tests_simple-tt</name>
  <url>https://github.com/dakusui/simple-tt</url>
  <properties>
    <opentest4j.version>1.2.0</opentest4j.version>
    <junit-jupiter-engine.version>5.10.1</junit-jupiter-engine.version>
    <junit-jupiter-api.version>5.10.1</junit-jupiter-api.version>
    <junit-platform-launcher.version>1.10.2</junit-platform-launcher.version>
    <insdog-all.version>0.4.8-SNAPSHOT</insdog-all.version>
    <!-- BEGIN: test dependencies -->
    <!-- END: test dependencies -->

  </properties>
  <dependencies>
    <dependency>
      <groupId>com.github.dakusui</groupId>
      <artifactId>insdog-all</artifactId>
      <version>${insdog-all.version}</version>
    </dependency>
    <dependency>
      <groupId>org.opentest4j</groupId>
      <artifactId>opentest4j</artifactId>
      <version>${opentest4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>${junit-jupiter-engine.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <version>${junit-jupiter-api.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <version>${junit-platform-launcher.version}</version>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <version>${junit-jupiter-api.version}</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <source>21</source>
          <target>21</target>
          <compilerArgs>
            -Aproject=${project.groupId}/${project.artifactId}
          </compilerArgs>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
